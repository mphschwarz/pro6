////////////////////////////////////////////////////////////////////////////////////////////
module SEL_1_BIT(
	input SEL,
	input IN1,
	input IN2,
	output wire OUT);
	//
	assign OUT = (IN1 && !SEL) || (IN2 && SEL);
	//
endmodule
////////////////////////////////////////////////////////////////////////////////////////////
module SEL_12_BIT(
	input SEL,
	input [11:0] IN1,
	input [11:0] IN2,
	output wire [11:0] OUT);
	//
	assign OUT = (IN1 && !SEL) || (IN2 && SEL);
	//
endmodule
////////////////////////////////////////////////////////////////////////////////////////////